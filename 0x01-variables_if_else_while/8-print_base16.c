#include <stdio.h>
/**
 * main - Entry poinrt 
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	int d;
	
	c = 'a';
	d = 0;
	while
		(d < 100) {
			putchar(d + '0');
			d++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
